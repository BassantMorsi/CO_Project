module mux2(pcPlusFour , ALUresult , andResult, newAddress);
input pcPlusFour,ALUresult;
output newAddress ;
input andResult;
reg newAddress;
always@(pcPlusFour or ALUresult or andResult)
begin
if (andResult==1'b0) newAddress=pcPlusFour ;
else newAddress=ALUresult;
end
endmodule
